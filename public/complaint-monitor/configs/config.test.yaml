# 测试环境配置文件

app:
  name: "complaint-monitor-test"
  environment: "test"
  log_level: "debug"

database:
  host: "mysql"
  port: 3306
  username: "third_party_payment"
  password: "rA8f@D2kLmZx!3pQ"
  database: "third_party_payment"
  max_open_conns: 10
  max_idle_conns: 5
  conn_max_lifetime: 3600

redis:
  host: "redis"
  port: 6379
  password: ""
  db: 0
  pool_size: 5

worker:
  refresh_interval: 30  # 测试环境缩短间隔
  fetch_interval: 1
  restartable: false    # 测试环境关闭自动重启

cert:
  cache_ttl: 300  # 测试环境缩短缓存时间
  encryption_key: "test_key_32bytes_1234567890abcde"  # 测试用密钥（32字节）

lock:
  base_ttl: 10
  max_ttl: 60

metrics:
  port: 19090  # 测试环境使用不同端口
  path: "/metrics"

health:
  port: 18080  # 测试环境使用不同端口
  path: "/health"

